#!/bin/bash

mkdir -p measures

echo filename,size > measures/binary-sizes
for file in `ls bin/*/*/*`; do
    size=`stat --printf="%s" ${file}`
    echo ${file},${size} >> measures/binary-sizes
done

cat measures/binary-sizes
